function mostrar()
{
	let edad;
	edad = parseInt(document.getElementById("txtIdEdad").value);

	if (edad >=13) and (edad <= 17) // && 
	{
		alert("Usted es adolescente")
	}
	
// andpersand && y pipe || or 
// o puedo poner un if adentro de otro if

// if(!(edad<13 ll edad>17))
}//FIN DE LA FUNCIÓN